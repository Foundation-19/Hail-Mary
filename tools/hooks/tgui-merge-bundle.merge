#!/bin/sh

# Keep the current branch's generated frontend bundles during conflicts.
# These artifacts are regenerated from source and should not be hand-merged.
echo "tgui-merge-bundle: keeping current branch version for $1"
exit 0
